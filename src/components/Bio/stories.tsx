import { Meta, StoryObj } from '@storybook/react'
import Bio from '.'

export default {
  title: 'Bio',
  component: Bio,
  parameters: {
    layout: 'fullscreen'
  }
} as Meta

export const Default: StoryObj = {}
