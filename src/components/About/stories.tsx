import { Meta, StoryObj } from '@storybook/react'
import About from './about'

export default {
  title: 'About',
  component: About,
  parameters: {
    layout: 'fullscreen'
  }
} as Meta

export const Default: StoryObj = {}
